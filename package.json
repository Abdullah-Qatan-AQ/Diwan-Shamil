{
  "name": "diwan-shamil",
  "version": "1.0.0",
  "private": true,
  "description": "الديوان الشامل — مكتبة عربية دون اتصال",
  "scripts": {
    "prepare:web": "bash scripts/prepare-web.sh",
    "android:sync": "npm run prepare:web && cap sync android",
    "android:open": "cap open android",
    "android:build": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "verify": "node --check app.js && node --check scripts/build-poetry-coverage.mjs && node --check scripts/build-poetry-coverage-shards.mjs && node --check scripts/verify-offline-lexicon.mjs",
    "quality:lexicon": "node scripts/filter-lexicon-quality.mjs && node scripts/build-lexicon-core.mjs",
    "build:lexicon-coverage": "node scripts/build-poetry-coverage.mjs && node scripts/build-poetry-coverage-shards.mjs",
    "verify:lexicon": "npm run build:lexicon-coverage && npm run quality:lexicon && node scripts/verify-offline-lexicon.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
